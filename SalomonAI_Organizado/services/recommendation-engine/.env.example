# Variables de entorno para el servicio recommendation-engine.
# Copia este archivo a `.env` y completa según el entorno.

# Dominios permitidos para peticiones CORS (separados por coma).
RECOMMENDATION_ENGINE_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Configuración opcional para control de acceso mediante API key.
# RECOMMENDATION_ENGINE_API_KEY=
