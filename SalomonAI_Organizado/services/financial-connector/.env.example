# Variables de entorno para el servicio financial-connector.
# Copia este archivo a `.env` y define los valores de acuerdo con tu entorno.

# Dominios permitidos para peticiones CORS (separados por coma).
FINANCIAL_CONNECTOR_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Clave API interna opcional si se expone el servicio.
# FINANCIAL_CONNECTOR_API_KEY=
